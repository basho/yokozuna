%% -*- erlang -*-
{application, yokozuna,
 [
  {description, "Integrating Apache Solr into Riak"},
  {vsn, {cmd, "git describe --always --tags | sed 's/-/-sentenai-/3'"}},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib,
                  ibrowse,
                  fuse,
                  riak_core,
                  riak_kv
                 ]},
  {mod, { yz_app, []}},
  {env, []}
 ]}.
